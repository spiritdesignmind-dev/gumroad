<div class="prose prose-lg max-w-none text-gray-800">
  <h2>Welcome to the Blog</h2>
  <p>This is the main content of my <strong>first blog post</strong>. It's written in HTML now.</p>
  <ul>
    <li>Item 1</li>
    <li>Item 2</li>
  </ul>
  <p>And a bit of <code>inline code</code>.</p>
  <pre><code class="language-ruby">
def hello
  puts "Hello, world!"
end
  </code></pre>
  <p>Also, create a placeholder image at <code>app/assets/images/blog/placeholder.jpg</code>. If you don't have one handy, just any small JPG will do for now.</p>
  <ol>
    <li> <strong>Update <code>Blog::PostsController</code>:</strong>
  Once the sample post is in place, we'll uncomment and fill in the logic in <code>Blog::PostsController</code> to use the <code>BlogService</code> methods to fetch data for the views.</li>
  </ol>
  <p>Let me know when you've created the sample Markdown file and placeholder image!</p>
</div>
